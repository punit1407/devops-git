git --version (check git version)

git config --global user.name "punit1407" (set user name globaly)
git config --global user.email "punit117aws@gmail.com" (set email globaly)


git config --list (check git config list)

mkdir devops-git-practice (Create directory)
cd devops-git-practice (go inside directory)


git init (initalize git config)

git status (git status)

ls -a (show hiden file)

cd .git

git add <file name> 

git commit -m "massge" (commit the changes)

git log

git diff  (what we have added in the file)

git log --oneline (log in oneline)

git restore rat.txt simran.txt (if we delet file form file system we restore in file system)

git switch dev (switch branch)

git checkout -d test (to delete branch)

git clone <HTTPS URL>

git remote -v (To check origin)
